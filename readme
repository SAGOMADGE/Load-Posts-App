# Load Posts App

Небольшое учебное SPA-приложение на чистом JavaScript, демонстрирующее работу с API, состоянием приложения и рендерингом UI без использования фреймворков.

## 🔹 Описание

Приложение загружает посты с публичного API и отображает их списком.  
Поддерживает постраничную загрузку, очистку данных и корректную обработку состояний интерфейса.

Проект сделан с упором на разделение логики:
- получение данных
- управление состоянием
- отображение UI

## 🔹 Функциональность

- Загрузка постов с API (`fetch`)
- Пагинация (load more)
- Global state (single source of truth)
- Empty state (когда данные отсутствуют)
- Loading state
- Управление состоянием кнопок
- Очистка данных (Delete all)

## 🔹 Стек технологий

- HTML5
- CSS3 (Flexbox)
- JavaScript (ES6+)
- Fetch API
- Модульная архитектура (ES modules)

## 🔹 Архитектура проекта

```text
src/
├── index.html      # HTML разметка
├── style.css       # Стили
├── index.js        # Точка входа
├── api.js          # Работа с API
├── state.js        # Управление состоянием
├── handler.js      # Обработчики событий и бизнес-логика
└── render.js       # Рендеринг UI
````

### Ключевые принципы

* `state.js` — единственный источник правды
* `handler.js` — управляет логикой и изменением state
* `render.js` — отвечает только за отображение UI
* UI обновляется **только после изменения state**

## 🔹 API

Используется публичное API:

```
https://dummyjson.com/posts
```

Загружаются только необходимые данные (`posts`, `total`).

## 🔹 Как запустить проект

1. Склонировать репозиторий
2. Открыть `index.html` в браузере
   (или использовать любой локальный сервер)

## 🔹 Чему я научился в этом проекте

* Работать с асинхронным JavaScript (`async / await`)
* Разделять ответственность между модулями
* Управлять состоянием приложения без фреймворков
* Реализовывать UI-состояния (empty, loading)
* Строить предсказуемый и читаемый код

---

Учебный проект, созданный для закрепления основ frontend-разработки.

```
